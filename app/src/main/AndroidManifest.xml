<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lucasfagundes.androidudemycourse">

    <application
        android:name=".app.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidUdemyCourse">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="locked"
            android:theme="@style/Theme.AndroidUdemyCourse.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".feature.atm_consultancy.AtmConsultancyActivity"
            android:label="@string/atm_consultancy"
            android:theme="@style/Theme.AtmConsultancy" />
        <activity
            android:name=".feature.media_player.activities.MediaPlayerActivity"
            android:label="@string/media_player"
            android:theme="@style/Theme.AndroidUdemyCourse.NoActionBar" />
        <activity
            android:name=".feature.media_player.activities.VideoPlayerActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AndroidUdemyCourse.NoActionBar" />

    </application>
</manifest>